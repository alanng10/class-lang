class Infra : Any
{
    maide prusate Bool Init()
    {
        base.Init();
        this.MathMath : share Math;
        return true;
    }

    field precate Math MathMath { get { return data; } set { data : value; } }

    maide prusate Int Int(var Comp comp, var Int value)
    {
        comp.Cand : value;
        comp.Expo : 0;

        var Int a;
        a : this.MathMath.Value(comp);
        return a;
    }
}