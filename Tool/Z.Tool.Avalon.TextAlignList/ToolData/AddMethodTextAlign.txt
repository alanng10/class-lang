    protected virtual #ItemClassName# AddItem(ulong horzAlign, ulong vertAlign)
    {
        #ItemClassName# item;
        item = new #ItemClassName#();
        item.Init();
        item.Index = this.Index;
        item.Intern = horzAlign | vertAlign;
        this.Array.SetAt(item.Index, item);
        this.Index = this.Index + 1;
        return item;
    }
