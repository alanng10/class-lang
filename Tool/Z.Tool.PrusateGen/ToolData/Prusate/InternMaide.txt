Int #Name#(Eval* eval, Int frame)
{
#Param#

    Int a;
    a = #Name#(#Argue#);

    Int ke;
    ke = 0;

    if (!(a == IntNull))
    {
        ke = a;
        RefKindSet(ke, RefKindInt);
    }

    Return(ke, #ParamCount#);
}