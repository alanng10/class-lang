    maide prudate Bool Init()
    {
        base.Init();
        this.InitArray();
        this.Count : this.Array.Count;
        this.Index : 0;

        var Extern varExtern;
        varExtern : share Extern;

        var Int varShare;
        varShare : varExtern.Infra_Share();
        var Int stat;
        stat : varExtern.Share_Stat(varShare);

#InitFieldList#        return true;
    }
