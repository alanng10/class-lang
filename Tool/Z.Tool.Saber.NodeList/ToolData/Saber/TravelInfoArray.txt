        this.Start(this.SField#ClassName#);

        this.FieldStart(this.SValue);
        
        this.StartArray();

        long count;
        count = #VarName#.Value.Count;
        var Int i;
        i = 0;
        while (i < count)
        {
            this.AddSpace();

            var #ItemDeclareClassName# item;
            item = cast #ItemDeclareClassName#(#VarName#.Value.Get(i));
            this.Execute#ItemClassName#(item);

            i = i + 1;
        }

        this.EndArray();

        this.FieldEnd("Value");

        this.End();
