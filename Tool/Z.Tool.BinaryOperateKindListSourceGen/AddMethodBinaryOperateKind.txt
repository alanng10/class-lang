    protected virtual #ItemClassName# AddItem(bool arg, bool anyArg)
    {
        #ItemClassName# item;
        item = new #ItemClassName#();
        item.Init();
        item.Index = this.Index;
        item.Arg = arg;
        item.AnyArg = anyArg;
        this.Array.Set(item.Index, item);
        this.Index = this.Index + 1;
        return item;
    }
